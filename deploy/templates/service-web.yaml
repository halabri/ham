apiVersion: v1
kind: Service
metadata:
  name: {{ include "ham.fullname" . }}-web
  labels:
    app.kubernetes.io/name: ham-web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: ham-web
  ports:
    - name: http
      port: 3000
      targetPort: http
